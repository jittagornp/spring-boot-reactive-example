# spring-boot-webflux-controller
Spring-boot WebFlux Controller example 


# Build & Run

cd to root of project directory 

``` shell 
$ mvn clean install

$ mvn spring-boot:run \
    -Dserver.port=8080
```

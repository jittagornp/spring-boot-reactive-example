# spring-boot-webflux-scheduling
Spring-boot WebFlux Scheduling example 


# Build & Run

cd to root of project directory 

``` shell 
$ mvn clean install

$ mvn spring-boot:run \
    -Dserver.port=8080
```
